-- This function is called by the C++ application to build the scene.
-- By leaving it empty, we create no objects and set no active camera,
-- which will cause the renderer to only draw the background shader.
function build_scene(scene)
	print("Building an empty scene for background display.")
	-- Intentionally do nothing here.
end
